import { IAddress } from "@multiversx/sdk-core/out/interface";
import { IContractQueryResponse } from "@multiversx/sdk-core/out/interfaceOfNetwork";
import { SmartContractQuery, SmartContractQueryResponse } from "@multiversx/sdk-core/out/smartContractQuery";
interface INetworkProvider {
    queryContract(query: IQuery): Promise<IContractQueryResponse>;
}
interface IQuery {
    address: IAddress;
    caller?: IAddress;
    func: {
        toString(): string;
    };
    value?: {
        toString(): string;
    };
    getEncodedArguments(): string[];
}
export declare class QueryRunnerAdapter {
    private readonly networkProvider;
    constructor(options: {
        networkProvider: INetworkProvider;
    });
    runQuery(query: SmartContractQuery): Promise<SmartContractQueryResponse>;
}
export {};
