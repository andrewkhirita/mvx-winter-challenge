import { EventDefinition } from "@multiversx/sdk-core/out/smartcontracts/typesystem/event";
import { TransactionEvent } from "@multiversx/sdk-core/out/transactionsOutcomeParsers/resources";
interface IAbi {
    getEvent(name: string): EventDefinition;
}
export declare class TransactionEventsParser {
    private readonly legacyResultsParser;
    private readonly abi;
    private readonly firstTopicIsIdentifier;
    constructor(options: {
        abi: IAbi;
        firstTopicIsIdentifier?: boolean;
    });
    parseEvents(options: {
        events: TransactionEvent[];
    }): any[];
    parseEvent(options: {
        event: TransactionEvent;
    }): any;
}
export {};
